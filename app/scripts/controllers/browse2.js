'use strict';

angular.module('clusterm.browse2', [])
    .controller('browse2Ctrl', ['$scope', '$state', 'ngDialog', function($scope, $state, ngd) {
        //agent.methods.CreateAgent($scope.user).$promise.then();
        $scope.ps = 8;
        /*$scope.search=agent.search;
        $scope.options=agent.soptions;
        $scope.sort="2";

        $scope.range = function(min, max, step){
            step = step || 1;
            var input = [];
            for (var i = min; i <= max; i += step) input.push(i);
            return input;
        };*/



    }]);
